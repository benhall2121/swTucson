<!DOCTYPE html>
<html>
  <head>
    <title>i tip artists</title>
    <!-- Fonts -->
    <link href='http://fonts.googleapis.com/css?family=Short+Stack' rel='stylesheet' type='text/css'>
    <link rel="stylesheet" type="text/css" href="/stylesheets/fancybox/jquery.fancybox-1.3.4.css">
    <%= stylesheet_link_tag "http://ajax.googleapis.com/ajax/libs/jqueryui/1.7.2/themes/redmond/jquery-ui.css", "application" %>
    <%= javascript_include_tag 'jquery', 'application' %>
    <%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.1/jquery-ui.min.js" %>
    <script type="text/javascript" src="/javascripts/fancybox/jquery.fancybox-1.3.4.pack.js"></script>
    <%= javascript_include_tag "http://www.google.com/jsapi" %>
    <%= javascript_include_tag 'rails' %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
  
    <div id="container">
      <div class="header">
        <div class="logo">
          <%= link_to image_tag("logo.png"), root_path %>
        </div>
     
        <div class="enter_div">
          <div class="header_top">
          
            <div class="loginout">
            <% if current_user %>
               <%= link_to "Log out", logout_path %>
            <% else %>
              <%= link_to "Log In", login_path %>
            <% end %>
            </div>
            
            <div class="supportTheArts">
              Support the Arts!
            </div>
          </div>
          
          <div class="nav">
          <div><%= link_to "HOME", root_path() %></div>
          <div><%= link_to "SEARCH FOR ARTISTS", users_path() %></div>
            <div><%= link_to "ABOUT US", about_us_path %></div>
            <% if current_user %>
              <div><%= link_to current_user.user_type.upcase + " PROFILE", user_path(current_user) %></div>
              <div><%= link_to "QR CODE", qr_code_path(current_user) %></div>
            <% end %>
          </div>
          
        </div>
      </div>
      
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
    </div>
  </body>
</html>
