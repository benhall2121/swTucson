<div>
<% @users.each do |user| %>
  <div class="user_search_div">
    <div><%= link_to (image_tag user.photo.url(:icon)), user_path(user) %></div>
    <div><%= link_to username(user), user_path(user) %></div>
  </div>
<% end %>
<%= will_paginate @users %>
</div>

